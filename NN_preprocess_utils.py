import numpy as np
import pandas as pd


def construct_feature(sub_path, feature_num, example_length, label_length):
    df = pd.read_csv(sub_path)


def normalization(X):
    return


def fit_dims(X, Y):
    return
